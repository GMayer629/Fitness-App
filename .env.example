# Vercel Postgres connection string (set automatically when you link a Vercel Postgres DB)
# For local dev, point this at your own Postgres instance
POSTGRES_URL=postgres://user:password@localhost:5432/fitlog

# Set to 'false' for local Postgres (no SSL); leave unset for Vercel (SSL auto-enabled via NODE_ENV)
# POSTGRES_SSL=false
